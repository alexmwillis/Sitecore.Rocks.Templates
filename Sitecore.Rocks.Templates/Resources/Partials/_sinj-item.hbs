var {{camelCase Name}}{{pascalCase TemplateName}}: SinjItemDto = { 
    id: "{{Id}}",
    name: "{{Name}}",
    template: "{{TemplatePath}}",
    parent: "{{ParentPath}}",
    language: "en",
{{#if Fields}}
    fields: {
        {{> sinj-fields Fields}}
    }
{{else}}
    fields: {}
{{/if}}
}