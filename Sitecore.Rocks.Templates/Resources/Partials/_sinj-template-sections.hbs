{{#each this}}
{
    name: "{{Name}}",
    id: "{{Id}}",
    sectionFields: {
        icon: "{{Icon}}"
    },
{{if Fields}}
    fields: [
        {{> sinj-template-fields Fields}}
    ]
{{else}}
    fields: []
{{/if}}
}{{#unless @last}},
{{/unless}}
{{/each}}