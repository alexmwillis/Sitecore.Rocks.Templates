var {{camelCase Name}}Template: SinjTemplateDto = { 
    id: "{{Id}}",
    name: "{{Name}}",
    parent: "{{ParentPath}}",
    fields: { 
        icon: "{{Icon}}",
        "Base template": "{{BaseTemplates}}"
    },
    sections: [
{{> sinj-template-sections Sections}}    ]{{#if StandardValues}},{{#with StandardValues}}
    standardValues: { 
        id: "{{Id}}",
        languages: ["en"],
        fields: { 
{{#where Fields 'Value'}}{{#where this 'Name' '^((?!__Owner)(?!__Created)(?!__Updated)(?!__Revision)(?!__Ribbon)(?!__Lock).)*$'}}{{#each this}}            "{{Name}}": "{{literal Value}}"{{#unless @last}},
{{/unless}}{{/each}}{{/where}}{{/where}}
        }
    }{{/with}}{{else}}{{/if}}
}